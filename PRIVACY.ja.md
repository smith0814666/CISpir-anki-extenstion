# CISpir プライバシーポリシー

**最終更新：2026 年 8 月**

CISpir はバックエンドを持たず、自社サーバーでユーザーデータを収集しません。このページでは、拡張機能が保存・送信する内容をありのまま説明します。

## お使いの端末に保存されるデータ

- **字幕テキスト、音声クリップ、動画メタデータ**は、AnkiConnect（`localhost:8765`）を通じてローカルの Anki にのみ送信され、パソコンの外に出ることはありません。
- **設定とプリファレンス**（Anki デッキ名、アクティブなタブ ID、寄付状態、AI 結果キャッシュなど）は、Chrome の `storage.local` API でローカルに保存されます。
- **API キー**は `storage.local` に保存される前に AES-GCM で暗号化されます。

## AI プロバイダに送信されるデータ

AI 機能（翻訳・文法解析・単語検索・動画要約）を使うと、字幕テキストや選択した文が**お使いのブラウザから直接**、設定した AI プロバイダ（OpenAI、Anthropic、Google AI、DeepSeek）に送信されます。CISpir が間にサーバーを介することはなく、リクエストはあなたの API キーで認証されます。キーを設定していない場合、またはこれらの機能を使わない場合は、何も送信されません。

## 匿名の利用統計（任意・デフォルトで無効）

拡張機能には任意の匿名利用統計（Google Analytics 4）が含まれています。**デフォルトでは無効**で、公開者が統計 ID を設定した場合のみ有効になります。有効な場合も、インストール/更新・日次アクティブ・機能利用など、集計された非個人情報のみを送信します。ランダムに生成された識別子を使用し、フィンガープリンティングは行わず、字幕テキストやアカウント情報は送信しません。

## サードパーティサービス

CISpir は広告やトラッキングサービスを一切統合していません。

## お問い合わせ

ご質問は [github.com/smith0814666/CISpir-anki-extenstion](https://github.com/smith0814666/CISpir-anki-extenstion) で Issue を作成してください。
